import Component from "../../../component";
import template from './zero-part.html'

export default class ZeroPart extends Component {
    mount({ anchor }) {
        super.mount({ anchor, template })
    }
}