import Component from '../../../component'
import template from './info-part.html'

export default class InfoPart extends Component {
    mount({ anchor }) {
        super.mount({ anchor, template })
    }
}